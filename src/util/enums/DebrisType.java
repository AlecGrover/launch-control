package util.enums;

/*
* An enum implemented in advance for future types of
* varied Debris. Currently does not serve a purpose
* beyond being a placeholder, but it allows for later
* expansion.
* */

public enum DebrisType {
    ASTEROID
}
